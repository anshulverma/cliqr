<%= @basename %><% unless @description.nil? || @description.empty? %> -- <%= @description %><% end %>

USAGE:
    <%= @basename %><% unless @options.empty? %> [options]

Available options:
<%
   @options.each { |option|
%>
    --<%= option.name %><% if option.short? %>, -<%= option.short %><% end %><% if option.description? %>  :  <%= option.description %><% end %>
<%
   }
%>
<% end %>
