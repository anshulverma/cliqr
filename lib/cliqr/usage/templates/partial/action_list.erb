<% if actions? %>
Available actions:
<%= "[ Type \"#{"#{command} " unless shell?}help [action-name]\" to get more information about that action ]\n" if help? %>
<%
   actions.each { |action|
%>    <%= action.name %><%= " -- #{action.description}" if action.description? %>
<%
   }
%>
<% end %>
